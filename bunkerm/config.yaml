---
name: BunkerM
version: 1.0.3
slug: bunkerm
description: MQTT Management Platform - Mosquitto broker with web-based management UI
url: https://github.com/hassos-apps/addon-bunkerm
image: ghcr.io/hassos-apps/bunkerm-{arch}
arch:
  - amd64
init: false
ingress: true
ingress_port: 2000
panel_icon: mdi:shield-lock
panel_title: BunkerM
map:
  - type: data
    read_only: false
ports:
  1900/tcp: 1900
  2000/tcp: null
ports_description:
  1900/tcp: MQTT Broker
  2000/tcp: Web UI (managed via Ingress)
options:
  mqtt_username: bunker
  mqtt_password: bunker
  log_level: INFO
schema:
  mqtt_username: str
  mqtt_password: password
  log_level: list(DEBUG|INFO|WARNING|ERROR)
startup: services
